import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "My Plug",
  description:
    "Connect with the right opportunities on [Your App Name]! Whether you're a job seeker looking for your next career move or an employer searching for top talent, our platform makes hiring fast and easy. Plus, buy and sell products effortlessly with our secure marketplace. Join thousands of users today and experience the ultimate hub for jobs and commerce!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
